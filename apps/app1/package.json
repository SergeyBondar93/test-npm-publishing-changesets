{
  "name": "app1",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --outfile=./dist/index.js"
  },
  "dependencies": {
    "@repo/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
