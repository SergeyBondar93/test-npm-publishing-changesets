{
  "name": "test-npm-publishing-changesets",
  "private": true,
  "scripts": {
    "clean": "node ./scripts/clean.js",
    "build": "npm run clean && turbo run build --force",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.1.7",
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.3.0",
  "workspaces": [
    "shared",
    "npm-package",
    "apps/*"
  ],
  "dependencies": {
    "esbuild": "^0.27.0"
  },
  "version": "0.0.0"
}
